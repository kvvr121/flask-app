apiVersion: v1
kind: Service
metadata:
  name: flask-app-service
  namespace: flask-app-staging
  labels:
    app: flask-app
    environment: staging
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5002
    protocol: TCP
    name: http
  selector:
    app: flask-app
